<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leehuang.his.api.db.dao.PermissionDao">


    <select id="getAllPermission"
            resultType="com.leehuang.his.api.mis.dto.role.vo.PermissionsVO">
        select p.id, m.module_name as moduleName, a.action_name as actionName
        from tb_permission p
                 left join tb_module m on p.module_id = m.id
                 left join tb_action a on p.action_id = a.id
        where p.id != 0
        order by m.id, p.id
    </select>
</mapper>
